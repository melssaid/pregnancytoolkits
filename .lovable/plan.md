# خطة تطوير شاملة: تحسين Onboarding + توسيع الحصة المجانية + تحسين شريط الاستهلاك

## الهدف

1. تحسين تجربة Onboarding لتوضيح قيمة التطبيق فوراً
2. زيادة الحد المجاني من 5 إلى 10 نقاط
3. تطوير شريط الاستهلاك ليكون أكثر احترافية مع رسم بياني وشرح واضح

---

## الخطوة 1: زيادة الحد المجاني من 5 إلى 10 نقاط

**الملف:** `src/services/smartEngine/types.ts`

- تغيير `QUOTA_TIERS.free.monthly` من `5` إلى `10`

**الملف:** `supabase/functions/pregnancy-ai-perplexity/index.ts`

- تحديث الحد المجاني في الـ Edge Function ليطابق `10`

**تحديث النصوص في كل اللغات** (`src/locales/*.json`):

- تحديث أي نص يذكر "5 تحليلات" إلى "10 تحليلات"

---

## الخطوة 2: تطوير Onboarding Step 1 — عنوان كبير وواضح وجذاب

**الملف:** `src/components/onboarding/OnboardingStep1Welcome.tsx`

التغييرات:

- **عنوان كبير وجذاب**: تكبير حجم العنوان إلى `text-lg font-black` مع خط Tajawal بوزن 800
- **شرح واضح للقيمة**: إضافة 3 نقاط سريعة (icons + text) توضح ما يقدمه التطبيق:
  - `🧠` تحليلات ذكية مجانية (10 تحليلات/شهر)
  - `🔒` خصوصية كاملة — بياناتك على جهازك فقط
  - `⚡` 36+ أداة احترافية
- **تكبير اللوجو** من `w-12 h-12` إلى `w-16 h-16` مع تأثير glow خفيف
- أضف استقصاء مخصص مثل تطبيق pregnancy الشهير، في حال اختيار اي قسم يكون هناك اسئلة مخصصة تفيد في ملئ بيانات المريض وتخزن محليا في 
- لا تجعل هناك ايقونات كثيرة او الوان متداخلة.. ليكن هادئ ومريح للعين وخطوط واضحة ومراعاة اللغات السبع

---

## الخطوة 3: تطوير شريط الاستهلاك (MiniUsageBar) — احترافي مع رسم بياني

**الملف:** `src/components/ai/MiniUsageBar.tsx`

التغييرات الرئيسية:

- **شريط أسمك وأوضح**: زيادة ارتفاع الشريط من `h-2` إلى `h-3` مع تدرج لوني واضح
- **أيقونة + إيماءة**: إضافة أيقونة `Zap` أو `Activity` بجانب الشريط مع pulse animation خفيف عند الاقتراب من الحد
- **شرح نصي واضح**: إضافة نص توضيحي أسفل الشريط يشرح نظام الاستهلاك بلغة بسيطة (مثل "كل تحليل ذكي يستهلك نقطة واحدة")
- **ربط بالخطة المدفوعة**: عند استهلاك 70%+، يظهر nudge خفيف "اشتركي للحصول على 60 تحليل/شهر" مع تدرج ذهبي

---

## الخطوة 4: تحسين شريط الاستهلاك في الصفحة الرئيسية (FooterCard)

**الملف:** `src/pages/Index.tsx`

- تحديث النصوص من "5" إلى "10" في كل المواضع
- تحسين شريط التقدم ليكون أسمك (`h-2` → `h-3`) مع gradient أوضح
- إضافة إيماءة بصرية (shimmer) عند اقتراب الاستنفاد

---

## الخطوة 5: تحديث UsageStatsNudge و TrialExpiryBanner

**الملفات:**

- `src/components/dashboard/UsageStatsNudge.tsx` — تحديث النص ليعكس 10 نقاط مجانية
- `src/components/TrialExpiryBanner.tsx` — لا تغيير (يعتمد على الوقت وليس النقاط)

---

## الملخص التقني


| التغيير                         | الملف                        |
| ------------------------------- | ---------------------------- |
| الحد المجاني: 5 → 10            | `types.ts` + Edge Function   |
| عنوان Onboarding كبير وجذاب     | `OnboardingStep1Welcome.tsx` |
| شريط استهلاك احترافي مع إيماءات | `MiniUsageBar.tsx`           |
| تحديث الصفحة الرئيسية           | `Index.tsx`                  |
| تحديث النصوص بكل اللغات         | `src/locales/*.json`         |
