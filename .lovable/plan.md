

## خطة حذف صفحة Badge Demo

سأقوم بتنظيف المشروع عبر:

1. **حذف ملف الصفحة**: `src/pages/BadgeDemo.tsx`
2. **إزالة المسار من الراوتر**: حذف سطر `import` و `Route` الخاص بـ `badge-demo` من `src/components/AnimatedRoutes.tsx`

